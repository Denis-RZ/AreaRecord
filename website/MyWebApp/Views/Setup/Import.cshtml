@{
    ViewData["Title"] = "Import";
}

<h1>Import Data</h1>

<p>The database schema is created automatically at startup using <code>EnsureCreated()</code>. To ensure the schema exists and seed sample data run:</p>

<pre><code>cd website/MyWebApp
dotnet run
</code></pre>

<p>Visit <code>/Setup</code> after startup to test the connection or insert the provided sample data.</p>

